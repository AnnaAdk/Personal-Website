const togglebutton = document.getElementById('togglebutton');
const naviList = document.getElementById('naviList');

togglebutton.addEventListener('click',() => {
    naviList.classList.togglebutton('active');
    })
